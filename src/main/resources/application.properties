# application.properties
# Application Name
spring.application.name=eventium

# Caffeine Cache System (if needed later)
# spring.cache.caffeine.spec=expireAfterAccess=20s
# spring.cache.cache-names=taskCache1, taskCache2

# Profiles (Only one profile should be active at a time)
# spring.profiles.active=dev
# spring.profiles.active=prod
# spring.profiles.active=rds

# Database Configuration
spring.datasource.url=jdbc:postgresql://ep-billowing-snowflake-a2sfzi43-pooler.eu-central-1.aws.neon.tech/neondb?sslmode=require
spring.datasource.username=neondb_owner
spring.datasource.password=npg_T9teRZ6lJLhO
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Globally configure Jackson to ignore lazy-loaded properties
spring.jackson.default-property-inclusion=non_null
spring.jackson.serialization.WRITE_DATES_AS_TIMESTAMPS=false
spring.jackson.serialization.FAIL_ON_EMPTY_BEANS=false

# Swagger UI
springdoc.api-docs.enabled=true
springdoc.swagger-ui.enabled=true
# Swagger UI Path (Optional: You can set the base path if needed)
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.api-docs.path=/v3/api-docs

# GitHub OAuth2 Authentication
spring.security.oauth2.client.registration.github.client-id=Ov23ligesyPFKE41HxTg
spring.security.oauth2.client.registration.github.client-secret=fd596a5dd00c7bb03a9c64237ab1d63e4651a416
spring.security.oauth2.client.registration.github.scope=read:user,user:email
spring.security.oauth2.client.registration.github.redirect-uri={baseUrl}/login/oauth2/code/{registrationId}
spring.security.oauth2.client.provider.github.authorization-uri=https://github.com/login/oauth/authorize
spring.security.oauth2.client.provider.github.token-uri=https://github.com/login/oauth/access_token
spring.security.oauth2.client.provider.github.user-info-uri=https://api.github.com/user